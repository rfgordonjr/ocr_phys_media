# ocr_phys_media
Experimentation with OCR on my collection of physical media

## Session Info

Python version: 3.12.3. It is recommended the user create a virtual python environment with `python3 -m venv. venv`.

`pip install Pillow pytesseract`
